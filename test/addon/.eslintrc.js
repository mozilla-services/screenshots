"use strict";

module.exports = {
  //"extends": [
  //  "plugin:mozilla/browser-test"
  //],

  // XXX Enable these rules (or remove from this list, when #2365 is fixed).
  "rules": {
    "no-undef": "off",
    "no-unused-vars": "off"
  }
};
